# Separated CPP Pipeline

These files were not moved because `REWRITE/separated/` is already a clean structured workspace with its own managed output folders.

Use:

- `REWRITE/separated/pytorch_train_cpp.py`
- `REWRITE/separated/pytorch_plot_cpp.py`

Outputs live under:

- `REWRITE/separated/outputs/cpp/`
